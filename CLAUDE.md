# CLAUDE.md

This repo is documented for agents in **[AGENTS.md](AGENTS.md)** — read that.

Claude Code: `./install.sh claude` symlinks these skills into `~/.claude/skills`; they then
load automatically and you invoke them by name.
